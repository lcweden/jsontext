{
  "name": "jsontext",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "imports": {
    "#src/*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc && vite build",
    "bench": "deno task bench",
    "dev": "vite",
    "format": "deno task format",
    "lint": "deno task lint",
    "preview": "vite preview",
    "test": "deno task test"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.9"
  }
}
